import {FsTools} from "./lib/mmk/Path";

FsTools.appTags = [18858, "app"];

App({
  globalData: {
    appTags: FsTools.appTags,
    offline: false,
  },
  onCreate(options) {
  },
  onDestroy(options) {
  }
});
